export type AccountFormType = {
  email: string;
  password: string;
  confirmPassword: string;
  username: string;
};

export type userInfoType = {
  email: string;
  username: string;
  createdAt: number;
  recentTravel: string;
};
