import styled from "styled-components";

const Form = styled.section`
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 0 16px;
  box-sizing: border-box;
`;

export const StConfirmTravelInfoSection = Object.assign({}, { Form });
