import styled from "styled-components";

const Wrapper = styled.header`
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 0 16px;
  box-sizing: border-box;
`;

export const StProgressHeader = Object.assign({}, { Wrapper });
