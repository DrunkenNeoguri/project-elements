import styled from "styled-components";

const Section = styled.section`
  display: flex;
  flex-direction: column;
  padding: 0 16px;
  box-sizing: border-box;
`;

export const StSignUpCompleteSection = Object.assign({}, { Section });
