/* transform: ${(props) =>
    props.$openState ? "translateX(0%)" : "translateX(100%)"};
  transition: transform 0.3s ease-in-out;

  filter: ${(props) =>
    props.$openState ? `drop-shadow(-8px 0 4px ${colors.shadow})` : `none`}; */

/* &:hover {
    background-color: ${colors.white};
    color: ${colors.black};
    transition: ease-in-out 0.2s;
    path {
      fill: ${colors.black};
    }
  } */
